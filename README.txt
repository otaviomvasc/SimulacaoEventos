Para instalar os pacotes do requiriments é necessário dar o seguinte
comando no terminal.

Com atenção na versão do python utilizada!!!!

pip install -r requirements.txt --no-deps -t python/lib/python3.9/site-packages/